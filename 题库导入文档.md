# 题库导入文档

## 概述

本文档用于指导AI（如豆包、ChatGPT等）识别题目图片并生成符合格式的题库JSON文件。

---

## 一、基本格式

```json
{
  "id": "题库唯一标识",
  "name": "题库名称",
  "description": "题库描述",
  "version": "1.0.0",
  "categories": ["分类1", "分类2"],
  "questions": [...]
}
```

### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | string | 是 | 唯一标识，用英文，如 `c-language`、`data-structure` |
| name | string | 是 | 题库中文名称 |
| description | string | 否 | 题库描述 |
| version | string | 否 | 版本号，默认 `1.0.0` |
| categories | array | 否 | 分类列表，用于筛选 |
| questions | array | 是 | 题目列表 |

---

## 二、题目格式

```json
{
  "id": 1,
  "type": "single",
  "category": "基础语法",
  "difficulty": 1,
  "question": "题目内容",
  "options": ["A. 选项1", "B. 选项2", "C. 选项3", "D. 选项4"],
  "answer": "A",
  "explanation": "解析说明"
}
```

### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | number | 是 | 题目序号，从1开始 |
| type | string | 是 | 题型：`single`(单选) / `multiple`(多选) / `judge`(判断) / `fill`(填空) |
| category | string | 否 | 所属分类 |
| difficulty | number | 否 | 难度：1-简单 2-中等 3-困难 |
| question | string | 是 | 题目内容 |
| options | array | 单选/多选必填 | 选项列表 |
| answer | string/array/boolean | 是 | 答案 |
| explanation | string | **是** | **解析说明（必写，简单易懂，方便记忆）** |

---

## 三、解析编写规范（重要！）

解析是刷题的核心，每道题**必须写**，要求：

### 3.1 解析三要素

| 要素 | 说明 | 示例 |
|------|------|------|
| **为什么** | 解释正确答案为什么对 | "因为变量名不能以数字开头" |
| **为什么错** | 解释错误选项为什么错 | "A错：以数字开头；C错：int是关键字" |
| **怎么记** | 口诀、技巧、联想记忆 | "变量命名三不：不数字开头、不用关键字、不用特殊符号" |

### 3.2 解析示例

**✅ 好的解析**：
```json
"explanation": "变量名规则：由字母、数字、下划线组成，不能以数字开头，不能是关键字。记忆口诀：'字母下划线开头，数字跟后不能先'。"
```

**❌ 差的解析**：
```json
"explanation": "答案是B。"
```

### 3.3 解析技巧

1. **一句话总结**：用一句话概括考点
2. **对比记忆**：把易混淆的知识点放在一起对比
3. **举例说明**：用具体例子帮助理解
4. **口诀记忆**：编顺口溜或口诀方便记忆

### 3.4 解析示例（按题型）

**单选题解析**：
```json
"explanation": "printf是输出函数，scanf是输入函数。记忆：'p'开头是print(打印/输出)，'s'开头是scan(扫描/输入)。"
```

**多选题解析**：
```json
"explanation": "基本数据类型有4种：int(整数)、float(浮点)、char(字符)、double(双精度浮点)。记忆口诀：'整浮字符双精度，四种基本要记住'。"
```

**判断题解析**：
```json
"explanation": "错误。int的大小取决于系统，16位系统占2字节，32/64位系统通常占4字节。记忆：'int大小看系统，标准只保不少于short'。"
```

**填空题解析**：
```json
"explanation": "常用格式说明符：%d-整数(integer)，%f-浮点(float)，%c-字符(character)，%s-字符串(string)。记忆：'d是整数f浮点，c字符s串连'。"
```

---

## 四、各题型示例

### 1. 单选题

```json
{
  "id": 1,
  "type": "single",
  "category": "基础语法",
  "difficulty": 1,
  "question": "以下哪个是C语言的合法变量名？",
  "options": ["A. 123abc", "B. _name", "C. int", "D. a-b"],
  "answer": "B",
  "explanation": "变量名规则：不能以数字开头，不能是关键字，只能包含字母、数字、下划线。"
}
```

**答案格式**：单个字母 `"A"` 或 `"B"` 等

### 2. 多选题

```json
{
  "id": 2,
  "type": "multiple",
  "category": "数据类型",
  "difficulty": 2,
  "question": "以下哪些是C语言的基本数据类型？",
  "options": ["A. int", "B. float", "C. string", "D. char"],
  "answer": ["A", "B", "D"],
  "explanation": "C语言没有内置string类型，字符串用字符数组表示。"
}
```

**答案格式**：字母数组 `["A", "B", "D"]`

### 3. 判断题

```json
{
  "id": 3,
  "type": "judge",
  "category": "基础语法",
  "difficulty": 1,
  "question": "C语言支持//单行注释和/* */多行注释。",
  "options": [],
  "answer": true,
  "explanation": "C99标准开始支持//单行注释。"
}
```

**答案格式**：`true`(正确) 或 `false`(错误)

### 4. 填空题

```json
{
  "id": 4,
  "type": "fill",
  "category": "基础语法",
  "difficulty": 2,
  "question": "在printf中，%d用于输出int类型，___用于输出float类型，___用于输出char类型。",
  "options": [],
  "answer": ["%f", "%c"],
  "explanation": "常用格式说明符：%d-整数，%f-浮点数，%c-字符，%s-字符串。"
}
```

**答案格式**：字符串数组，按空出现顺序 `["答案1", "答案2"]`

---

## 五、代码格式规范（重要！）

### 4.1 题目中包含代码

在 `question` 字段中使用反引号包裹代码：

```json
{
  "question": "以下代码的输出结果是？\n```c\nint a = 5;\ndouble b = a / 2;\nprintf(\"%.1f\", b);\n```",
  "options": ["A. 2.5", "B. 2.0", "C. 3.0", "D. 编译错误"]
}
```

**关键点**：
- 代码块用 ` ```c ` 开头，` ``` ` 结尾
- 代码中的换行用 `\n` 表示
- 代码中的引号 `"` 要转义为 `\"`
- 代码中的反引号 `` ` `` 要转义为 `` \` ``

### 4.2 选项中包含代码

用反引号包裹代码：

```json
{
  "options": [
    "A. `int arr[10];`",
    "B. `int arr(10);`",
    "C. `int arr[] = 10;`",
    "D. `int[10] arr;`"
  ]
}
```

### 4.3 解析中包含代码

```json
{
  "explanation": "正确写法：\n```c\nint arr[10];\n```\n\n注意使用方括号[]，不是圆括号()。"
}
```

### 4.4 代码中的特殊字符转义

| 字符 | 转义写法 |
|------|----------|
| 换行 | `\n` |
| 双引号 | `\"` |
| 反斜杠 | `\\` |
| 制表符 | `\t` |

### 4.5 完整的代码题示例

```json
{
  "id": 5,
  "type": "single",
  "category": "控制结构",
  "difficulty": 2,
  "question": "以下代码执行后，sum的值是？\n```c\nint sum = 0;\nfor (int i = 1; i <= 100; i++) {\n    sum += i;\n}\n```",
  "options": ["A. 4950", "B. 5000", "C. 5050", "D. 5100"],
  "answer": "C",
  "explanation": "等差数列求和：1 + 2 + 3 + ... + 100 = (100 × 101) / 2 = 5050"
}
```

---

## 六、数学公式规范

### 5.1 行内公式

用单个 `$` 包裹：

```json
{
  "question": "轴向拉压变形公式 $\\Delta L = \\frac{FL}{EA}$ 中，E代表什么？"
}
```

### 5.2 块级公式

用双 `$` 包裹：

```json
{
  "explanation": "弯曲正应力公式：\n$$\\sigma = \\frac{My}{I}$$\n\n其中y是到中性轴的距离。"
}
```

### 5.3 常用LaTeX语法

| 公式 | LaTeX写法 |
|------|-----------|
| 上标 | `x^2` |
| 下标 | `F_{max}` |
| 分数 | `\frac{a}{b}` |
| 根号 | `\sqrt{x}` |
| 希腊字母 | `\alpha` `\beta` `\sigma` `\Delta` |
| 求和 | `\sum_{i=1}^{n}` |
| 积分 | `\int_{a}^{b}` |
| 约等 | `\approx` |
| 不等 | `\neq` `\geq` `\leq` |

---

## 七、完整题库示例

```json
{
  "id": "c-language-basics",
  "name": "C语言基础",
  "description": "C语言程序设计基础题库",
  "version": "1.0.0",
  "categories": ["基础语法", "数据类型", "控制结构", "数组", "指针", "函数"],
  "questions": [
    {
      "id": 1,
      "type": "single",
      "category": "基础语法",
      "difficulty": 1,
      "question": "以下哪个是C语言的合法标识符？",
      "options": ["A. 123var", "B. _count", "C. int", "D. my-var"],
      "answer": "B",
      "explanation": "标识符命名规则：由字母、数字、下划线组成，不能以数字开头，不能是关键字。"
    },
    {
      "id": 2,
      "type": "single",
      "category": "数据类型",
      "difficulty": 2,
      "question": "以下代码的输出结果是？\n```c\nint a = 5;\ndouble b = a / 2;\nprintf(\"%.1f\", b);\n```",
      "options": ["A. 2.5", "B. 2.0", "C. 3.0", "D. 编译错误"],
      "answer": "B",
      "explanation": "5 / 2是整数除法，结果为2，赋值给double变为2.0。要得到2.5需要5.0 / 2。"
    },
    {
      "id": 3,
      "type": "judge",
      "category": "基础语法",
      "difficulty": 1,
      "question": "int类型在所有系统上都占4字节。",
      "options": [],
      "answer": false,
      "explanation": "int的大小取决于系统架构，标准只保证sizeof(int) >= sizeof(short)。"
    }
  ]
}
```

---

## 八、AI识别题目时的提示词模板

将以下提示词发送给AI，附上题目图片：

```
请识别图片中的题目，按照以下JSON格式生成题库：

1. 每道题包含：id(序号)、type(题型)、question(题目)、options(选项)、answer(答案)、explanation(解析)
2. 题型：single(单选)、multiple(多选)、judge(判断)、fill(填空)
3. 代码用 ```语言 包裹，换行用 \n
4. 答案格式：单选填字母，多选填数组，判断填true/false
5. 选项格式：["A. xxx", "B. xxx", ...]
6. 解析必须写，要求：简单易懂，方便记忆，包含记忆口诀或技巧

输出完整的JSON格式题库，id为"c-language"，name为"C语言基础"。
```

---

## 九、注意事项

1. **JSON格式必须合法**：所有字符串用双引号，不能有尾随逗号
2. **代码换行**：必须用 `\n`，不能直接换行
3. **引号转义**：代码中的 `"` 必须写成 `\"`
4. **答案准确性**：确保答案正确，解析清晰
5. **题目唯一性**：每道题的id在题库内唯一
6. **解析必写**：每道题必须有解析，要求简单易懂、方便记忆

---

## 十、导入方式

1. 将生成的JSON保存为 `.json` 文件
2. 放入 `banks/` 目录
3. 在 `js/app.js` 的 `builtinBanks` 数组中添加文件名
4. 重新构建部署
