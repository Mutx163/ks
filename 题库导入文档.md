# 题库导入文档

## 概述

本文档用于指导AI（如豆包、ChatGPT等）识别题目图片并生成符合格式的题库JSON文件。

当前项目生产环境只使用 Cloudflare Worker/D1 中的云端题库。`banks/*.json` 仅用于人工备份，不参与前端运行时加载，也不会作为 API 失败时的兜底来源。新增正式题库必须通过 `admin.html` 管理后台导入并启用。

---

## 一、基本格式

```json
{
  "id": "题库唯一标识",
  "name": "题库名称",
  "description": "题库描述",
  "version": "1.0.0",
  "categories": ["分类1", "分类2"],
  "questions": [...]
}
```

### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | string | 是 | 唯一标识，用英文，如 `c-language`、`data-structure` |
| name | string | 是 | 题库中文名称 |
| description | string | 否 | 题库描述 |
| version | string | 否 | 版本号，默认 `1.0.0` |
| categories | array | 否 | 分类列表，用于筛选 |
| questions | array | 是 | 题目列表 |

---

## 二、题目格式

```json
{
  "id": 1,
  "type": "single",
  "category": "基础语法",
  "difficulty": 1,
  "question": "题目内容",
  "options": ["A. 选项1", "B. 选项2", "C. 选项3", "D. 选项4"],
  "answer": "A",
  "explanation": "解析说明"
}
```

### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | number | 是 | 题目序号，从1开始 |
| type | string | 是 | 题型：`single`(单选) / `multiple`(多选) / `judge`(判断) / `fill`(填空) / `code`(编程) / `essay`(简答) |
| category | string | 建议必填 | 所属知识点分类；学习分析页用它统计薄弱知识点，缺失时会归为“未分类” |
| difficulty | number | 否 | 难度：1-简单 2-中等 3-困难 4-很难 5-极难 |
| question | string | 是 | 题目内容 |
| options | array | 单选/多选必填 | 选项列表，支持字符串或对象格式 |
| answer | string/array/boolean | 是 | 答案 |
| explanation | string | **是** | **解析说明（必写，简单易懂，方便记忆）** |
| tags | array | 否 | 题目标签，当前主要用于扩展和导出 |
| img / image | string | 否 | 题目图片地址 |
| code | string | 否 | 解析区展示的参考代码 |
| codeLanguage | string | 否 | 代码语言，如 `c`、`cpp`、`java`、`python`、`javascript` |
| memoryAid | string | 否 | 记忆提示，会显示在解析区 |

### 图片支持说明

系统支持在题目和选项中显示图片：

**1. 题目图片**：通过 `img` 或 `image` 字段指定图片URL

```json
{
  "question": "以下哪个是正确的电路图？",
  "img": "https://cdn.example.com/circuit.png"
}
```

**2. 选项图片**：选项支持对象格式，包含 `text` 和 `img` 字段

```json
{
  "options": [
    {"text": "选项A的描述", "img": "https://cdn.example.com/opt-a.png"},
    {"text": "选项B的描述", "img": "https://cdn.example.com/opt-b.png"},
    "选项C（纯文本）",
    "选项D（纯文本）"
  ]
}
```

**3. 解析图片**：在解析中使用Markdown图片语法 `![描述](URL)`

```json
{
  "explanation": "正确答案是A。如图所示：\n![电路图解析](https://cdn.example.com/explain.png)"
}
```

> ⚠️ **注意**：AI 生成题目时无法处理图片。如果题目需要图片，请：
> 1. 先用 AI 生成文本题目
> 2. 导入系统后，在后台管理中手动上传图片
> 3. 后台提供图片上传功能，支持题目图片和选项图片

---

## 三、解析编写规范（重要！）

解析是刷题的核心，每道题**必须写**，要求：

### 3.1 解析三要素

| 要素 | 说明 | 示例 |
|------|------|------|
| **为什么** | 解释正确答案为什么对 | "因为变量名不能以数字开头" |
| **为什么错** | 解释错误选项为什么错 | "123abc错：以数字开头；int错：是关键字" |
| **怎么记** | 口诀、技巧、联想记忆 | "变量命名三不：不数字开头、不用关键字、不用特殊符号" |

> ⚠️ **解析中不要用 A/B/C/D 指代选项**，要用选项内容本身。系统支持选项乱序，开启后选项顺序会打乱，用字母指代会对应不上。例如：\n- ✅ 正确："`_name` 是合法的，`123abc` 错在以数字开头"\n- ❌ 错误："A 错在以数字开头，B 是正确答案"

### 3.2 解析示例

**✅ 好的解析**：
```json
"explanation": "变量名规则：由字母、数字、下划线组成，不能以数字开头，不能是关键字。`_name` 符合规则；`123abc` 错在以数字开头；`int` 是关键字；`a-b` 含非法字符。"
```

**❌ 差的解析**：
```json
"explanation": "答案是B。A错，C错，D也错。"
```

### 3.3 解析技巧

1. **一句话总结**：用一句话概括考点
2. **对比记忆**：把易混淆的知识点放在一起对比
3. **举例说明**：用具体例子帮助理解
4. **口诀记忆**：编顺口溜或口诀方便记忆

### 3.4 解析示例（按题型）

**单选题解析**：
```json
"explanation": "`printf` 是输出函数，`scanf` 是输入函数。记忆：'p'开头是print(打印/输出)，'s'开头是scan(扫描/输入)。"
```

**多选题解析**：
```json
"explanation": "基本数据类型有4种：`int`(整数)、`float`(浮点)、`char`(字符)、`double`(双精度浮点)。记忆口诀：'整浮字符双精度，四种基本要记住'。`string` 不是基本类型，C语言用字符数组表示字符串。"
```

**判断题解析**：
```json
"explanation": "错误。int的大小取决于系统，16位系统占2字节，32/64位系统通常占4字节。记忆：'int大小看系统，标准只保不少于short'。"
```

**填空题解析**：
```json
"explanation": "常用格式说明符：%d-整数(integer)，%f-浮点(float)，%c-字符(character)，%s-字符串(string)。记忆：'d是整数f浮点，c字符s串连'。"
```

---

## 四、各题型示例

### 1. 单选题

```json
{
  "id": 1,
  "type": "single",
  "category": "基础语法",
  "difficulty": 1,
  "question": "以下哪个是C语言的合法变量名？",
  "options": ["A. 123abc", "B. _name", "C. int", "D. a-b"],
  "answer": "B",
  "explanation": "变量名规则：不能以数字开头，不能是关键字，只能包含字母、数字、下划线。`123abc` 以数字开头不合法，`_name` 符合规则，`int` 是关键字，`a-b` 含减号不合法。"
}
```

**答案格式**：单个字母 `"A"` 或 `"B"` 等

### 2. 多选题

```json
{
  "id": 2,
  "type": "multiple",
  "category": "数据类型",
  "difficulty": 2,
  "question": "以下哪些是C语言的基本数据类型？",
  "options": ["A. int", "B. float", "C. string", "D. char"],
  "answer": ["A", "B", "D"],
  "explanation": "C语言没有内置string类型，字符串用字符数组表示。`int`、`float`、`char` 都是基本数据类型，`string` 不是。"
}
```

**答案格式**：字母数组 `["A", "B", "D"]`

### 3. 判断题

```json
{
  "id": 3,
  "type": "judge",
  "category": "基础语法",
  "difficulty": 1,
  "question": "C语言支持//单行注释和/* */多行注释。",
  "options": [],
  "answer": true,
  "explanation": "C99标准开始支持//单行注释。"
}
```

**答案格式**：`true`(正确) 或 `false`(错误)

### 4. 填空题

```json
{
  "id": 4,
  "type": "fill",
  "category": "基础语法",
  "difficulty": 2,
  "question": "在printf中，%d用于输出int类型，___用于输出float类型，___用于输出char类型。",
  "options": [],
  "answer": ["%f", "%c"],
  "explanation": "常用格式说明符：%d-整数，%f-浮点数，%c-字符，%s-字符串。"
}
```

**答案格式**：字符串数组，按空出现顺序 `["答案1", "答案2"]`

### 5. 编程题

```json
{
  "id": 5,
  "type": "code",
  "category": "程序设计",
  "difficulty": 3,
  "question": "编写程序，输入两个整数，输出它们的和。",
  "options": [],
  "answer": "#include <stdio.h>\nint main() {\n    int a, b;\n    scanf(\"%d %d\", &a, &b);\n    printf(\"%d\", a + b);\n    return 0;\n}",
  "explanation": "使用scanf读取两个整数，再用printf输出两数之和。",
  "codeLanguage": "c"
}
```

**答案格式**：参考代码字符串。当前系统不会自动运行代码，提交非空代码后默认进入解析。

### 6. 简答题

```json
{
  "id": 6,
  "type": "essay",
  "category": "概念理解",
  "difficulty": 2,
  "question": "简述数组和指针的关系。",
  "options": [],
  "answer": "数组名在多数表达式中会转换为指向首元素的指针，但数组本身不是指针。",
  "explanation": "记忆：数组是一片连续空间，指针是保存地址的变量。数组名常会退化为首元素地址。"
}
```

**答案格式**：参考答案字符串。答题页会先显示参考答案，再由用户自评“答对/答错”。

---

## 五、学习趋势/分析适配说明

新增的“学习趋势”和“学习分析”页面不要求题库增加额外字段，但题库字段会影响分析质量：

| 分析模块 | 依赖字段 | 说明 |
|----------|----------|------|
| 14日趋势 | 无需导入字段 | 根据用户答题后生成的 `answeredAt` 进度记录统计 |
| 题型掌握 | `type` | 按单选、多选、判断、填空、编程、简答统计掌握情况 |
| 薄弱知识点 | `category` | 按知识点分类统计正确率；建议每题都填写 |
| 题库分析 | `id`、`name`、`questions`、`category` | 展示题库进度、正确率和分类弱项 |
| 错题/复习 | 稳定的 `id` | 题目 id 改动会导致旧进度、错题、复习记录对不上 |

为了让分析页准确工作，建议：

1. `id` 在同一题库内唯一且后续版本保持稳定
2. `type` 只使用标准英文值：`single`、`multiple`、`judge`、`fill`、`code`、`essay`
3. `category` 不要留空，尽量使用稳定的知识点名称
4. 同一知识点不要混用多个近似名称，例如“基础语法”和“语法基础”应统一
5. `difficulty` 建议使用 1-5，方便后续扩展难度分析

---

## 六、代码格式规范（重要！）

### 6.1 题目中包含代码

在 `question` 字段中使用反引号包裹代码：

```json
{
  "question": "以下代码的输出结果是？\n```c\nint a = 5;\ndouble b = a / 2;\nprintf(\"%.1f\", b);\n```",
  "options": ["A. 2.5", "B. 2.0", "C. 3.0", "D. 编译错误"]
}
```

**关键点**：
- 代码块用 ` ```c ` 开头，` ``` ` 结尾
- 代码中的换行用 `\n` 表示
- 代码中的引号 `"` 要转义为 `\"`
- 代码中的反引号 `` ` `` 要转义为 `` \` ``

### 6.2 选项中包含代码

用反引号包裹代码：

```json
{
  "options": [
    "A. `int arr[10];`",
    "B. `int arr(10);`",
    "C. `int arr[] = 10;`",
    "D. `int[10] arr;`"
  ]
}
```

### 6.3 解析中包含代码

```json
{
  "explanation": "正确写法：\n```c\nint arr[10];\n```\n\n注意使用方括号[]，不是圆括号()。"
}
```

### 6.4 代码中的特殊字符转义

| 字符 | 转义写法 |
|------|----------|
| 换行 | `\n` |
| 双引号 | `\"` |
| 反斜杠 | `\\` |
| 制表符 | `\t` |

### 6.5 完整的代码题示例

```json
{
  "id": 5,
  "type": "single",
  "category": "控制结构",
  "difficulty": 2,
  "question": "以下代码执行后，sum的值是？\n```c\nint sum = 0;\nfor (int i = 1; i <= 100; i++) {\n    sum += i;\n}\n```",
  "options": ["A. 4950", "B. 5000", "C. 5050", "D. 5100"],
  "answer": "C",
  "explanation": "等差数列求和：1 + 2 + 3 + ... + 100 = (100 × 101) / 2 = 5050"
}
```

---

## 七、数学公式规范

### 7.1 行内公式

用单个 `$` 包裹：

```json
{
  "question": "轴向拉压变形公式 $\\Delta L = \\frac{FL}{EA}$ 中，E代表什么？"
}
```

### 7.2 块级公式

用双 `$` 包裹：

```json
{
  "explanation": "弯曲正应力公式：\n$$\\sigma = \\frac{My}{I}$$\n\n其中y是到中性轴的距离。"
}
```

### 7.3 常用LaTeX语法

| 公式 | LaTeX写法 |
|------|-----------|
| 上标 | `x^2` |
| 下标 | `F_{max}` |
| 分数 | `\frac{a}{b}` |
| 根号 | `\sqrt{x}` |
| 希腊字母 | `\alpha` `\beta` `\sigma` `\Delta` |
| 求和 | `\sum_{i=1}^{n}` |
| 积分 | `\int_{a}^{b}` |
| 约等 | `\approx` |
| 不等 | `\neq` `\geq` `\leq` |

---

## 八、完整题库示例

```json
{
  "id": "c-language-basics",
  "name": "C语言基础",
  "description": "C语言程序设计基础题库",
  "version": "1.0.0",
  "categories": ["基础语法", "数据类型", "控制结构", "数组", "指针", "函数"],
  "questions": [
    {
      "id": 1,
      "type": "single",
      "category": "基础语法",
      "difficulty": 1,
      "question": "以下哪个是C语言的合法标识符？",
      "options": ["A. 123var", "B. _count", "C. int", "D. my-var"],
      "answer": "B",
      "explanation": "标识符命名规则：由字母、数字、下划线组成，不能以数字开头，不能是关键字。`123var` 以数字开头，`int` 是关键字，`my-var` 含减号，只有 `_count` 合法。"
    },
    {
      "id": 2,
      "type": "single",
      "category": "数据类型",
      "difficulty": 2,
      "question": "以下代码的输出结果是？\n```c\nint a = 5;\ndouble b = a / 2;\nprintf(\"%.1f\", b);\n```",
      "options": ["A. 2.5", "B. 2.0", "C. 3.0", "D. 编译错误"],
      "answer": "B",
      "explanation": "5 / 2 是整数除法，结果为 2，赋值给 double 变为 2.0。要得到 2.5 需要 `5.0 / 2`。"
    },
    {
      "id": 3,
      "type": "judge",
      "category": "基础语法",
      "difficulty": 1,
      "question": "int类型在所有系统上都占4字节。",
      "options": [],
      "answer": false,
      "explanation": "int的大小取决于系统架构，标准只保证sizeof(int) >= sizeof(short)。"
    }
  ]
}
```

---

## 九、AI识别题目时的提示词模板

将以下提示词发送给AI，附上题目图片：

```
请识别图片中的题目，按照以下JSON格式生成题库：

1. 每道题包含：id(序号)、type(题型)、question(题目)、options(选项)、answer(答案)、explanation(解析)
2. 题型：single(单选)、multiple(多选)、judge(判断)、fill(填空)、code(编程)、essay(简答)
3. 代码用 ```语言 包裹，换行用 \n
4. 答案格式：单选填字母，多选填数组，判断填true/false
5. 选项格式：["A. xxx", "B. xxx", ...]
6. 解析必须写，要求：简单易懂，方便记忆
7. ⚠️ 解析中禁止用 A/B/C/D 指代选项，要用选项的实际内容。例如写“`_name` 是合法标识符”，不要写“B 是正确答案”。因为系统有选项乱序功能，字母顺序会变。

输出完整的JSON格式题库，id为"c-language"，name为"C语言基础"。
```

---

## 十、注意事项

1. **JSON格式必须合法**：所有字符串用双引号，不能有尾随逗号
2. **代码换行**：必须用 `\n`，不能直接换行
3. **引号转义**：代码中的 `"` 必须写成 `\"`
4. **答案准确性**：确保答案正确，解析清晰
5. **题目唯一性**：每道题的id在题库内唯一
6. **解析必写**：每道题必须有解析，要求简单易懂、方便记忆
7. **解析禁用字母指代**：解析中不要用 A/B/C/D 指代选项，要引用选项的实际内容。系统支持选项乱序，开启后显示顺序会打乱，用字母会导致对应不上

---

## 十一、导入方式

### 生产环境导入

1. 将生成的 JSON 保存为 `.json` 文件
2. 打开 `admin.html` 管理后台
3. 通过后台导入题库并确认启用
4. 重新打开首页，题库应从 Cloudflare Worker/D1 API 加载

### 本地备份

1. 可以把 JSON 文件另存到 `banks/` 目录作为人工备份
2. 如需记录备份清单，可更新 `js/config.js` 的 `backupBankFiles`
3. 注意：备份文件不会被前端加载，也不会被构建复制到生产产物
